//arrays e obkjetos (medo)

let meta = ["estudar", "hoje"]

console.log(meta[0] + " " + meta[1])

// Quando você junta dois valores, é contatemação

//viu que daora? voce chamou por meio da posição dele??? aaaaa mulek

// objetos,os com as chgaves {}

let metas = {
    value: "ler 1 livro por mês",
    checked: false,
    oi: (info) => {
        console.log(info)
    }
}

metas.oi(metas.value)

//não sei como raios isso deu certo, mas é uma das formas da função

//function: //arrow function, atribuo com as setinhas

// metas.value = "nao"
